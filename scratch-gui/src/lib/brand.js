// Legacy export format because this is used by some build-time scripts stuck in the past.
// eslint-disable-next-line import/no-commonjs
module.exports = {
    // "Scratch" is a registered trademark of the Scratch Team / MIT, and the
    // GPL grants no trademark rights — so the product carries its own name and
    // credits Scratch descriptively instead.
    APP_NAME: 'Squiggle'
};
